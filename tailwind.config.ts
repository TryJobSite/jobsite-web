module.exports = {
  theme: {
    extend: {
      fontFamily: {
        // override the default `sans` family to prefer Helvetica Neue
        sans: ['"Helvetica Neue Local"', 'ui-sans-serif', 'system-ui', 'sans-serif'],
      },
    },
  },
  plugins: [],
};
